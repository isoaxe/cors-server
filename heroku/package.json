{
  "name": "cors-server",
  "version": "1.1.1",
  "description": "A reverse proxy that adds CORS headers to a HTTP request",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch": "nodemon server.js",
    "deploy-heroku": "git push heroku master --force",
    "deploy-fly": "fly deploy"
  },
  "author": "Lucas O'Connell",
  "license": "ISC",
  "dependencies": {
    "cors-anywhere": "^0.4.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
